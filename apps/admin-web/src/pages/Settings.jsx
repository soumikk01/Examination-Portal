const Settings = () => {
  return (
    <div className="admin-card">
      <h1 style={{ marginBottom: '0.5rem', fontSize: '1.35rem' }}>Settings</h1>
      <p style={{ marginTop: 0, color: 'var(--admin-text-muted)' }}>
        Settings module is not configured yet.
      </p>
    </div>
  );
};

export default Settings;

